<%= form_for(@ventas_pedido) do |f| %>
  <% if @ventas_pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ventas_pedido.errors.count, "error") %> prohibited this ventas_pedido from being saved:</h2>

      <ul>
      <% @ventas_pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :cliente_id %><br>
    <%= f.number_field :cliente_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
